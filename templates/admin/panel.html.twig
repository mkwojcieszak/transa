{% extends 'admin_base.html.twig' %}

{% block body %}
    <h3>Panel Administratora</h3>

    <a href='{{ path('admin.posty') }}'><button class='btn btn-primary'>Posty</button></a>
    <a href='{{ path('admin.trasy') }}'><button class='btn btn-primary'>Trasy</button></a>
{% endblock %}
